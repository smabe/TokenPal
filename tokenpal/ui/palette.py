"""Shared color constants for buddy rendering.

Kept as plain hex strings so Qt (`QColor`), Tkinter (raw), Textual TCSS,
and ANSI escapes can each consume them in their own way without a
central dependency.
"""

BUDDY_GREEN = "#00ff88"
