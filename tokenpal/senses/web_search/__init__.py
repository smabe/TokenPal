"""Web search backend package for /ask — NOT a sense.

Real implementation lives in `client.py`. This package is intentionally
not decorated with `@register_sense` — it holds backend code for the
user-triggered `/ask` slash command, not passive observations.
"""
