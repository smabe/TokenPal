"""Audio backend discovery target.

Modules here are imported by ``tokenpal.audio.registry.discover_backends``.
Input-side backend modules MUST be prefixed ``asr_`` or ``wake_`` so the
include_input gate works mechanically.
"""
