# Resume prompts

One line per active plan. After `/clear`, send the `/plan <slug>` command shown — it validates the plan against HEAD and runs the phase cycle. Do not implement inline.
- find-files-open-path — #53, p1-p3 shipped (`f0ca2f0`, `d42522f`, `3bbe847`), NEXT p4 (Windows Search, unverified on Windows); send `/plan find-files-open-path`
